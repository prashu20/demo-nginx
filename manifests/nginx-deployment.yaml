apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-httpd
  namespace: demo-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-httpd
  template:
    metadata:
      labels:
        app: demo-httpd
    spec:
      containers:
      - name: httpd
        image: httpd:alpine
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: demo-httpd
  namespace: demo-nginx
spec:
  selector:
    app: demo-httpd
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: demo-httpd
  namespace: demo-nginx
spec:
  to:
    kind: Service
    name: demo-httpd
  port:
    targetPort: 80
  tls:
    termination: edge

